name: mlops-application
on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: my-step
        run: echo "Hello World mlops-application!"
      
      - name: Install Python
        run:  echo "Installing Python"